// function toplama(x, y) {
//   return x + y;
// }

// const toplama = (x, y) => x + y;

// const toplam = toplama(10, 5);

const getFullname = (firstname, surname) => firstname + " " + surname ;

const getString = () => {
  return "kodluyoruz";
};

console.log(getString());
